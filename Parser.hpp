#ifndef _L_PARSER
#define _L_PARSER

#include <iostream>
#include <fstream>
#include <string>
#include <map>

class Parser {
private:
public:
    void Parse(std::string filename);
    Parser() {}
    ~Parser() {}
    
};
#endif
